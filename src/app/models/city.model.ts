export class City {
  constructor(public name: string, public url: string, public county: string[], public state: string, public lat: number, public lon: number, public radar: string, public station: string, public wfo: string, public region: string) {

  }
}
